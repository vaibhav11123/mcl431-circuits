# 2017 indexing drill — SKIP

`dcv_5_3` is in `lecture/atlas.yaml` with `crop: null` (L10A p16 names 5/3 but has no isolated glyph).

Do not draw this paper until a real L10A 5/3 crop exists. No generic ISO substitute.
