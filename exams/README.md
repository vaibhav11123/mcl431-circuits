Previous-year papers. How the exam asks — not how to draw.

- `pyqs/` — raw PDFs and images
- `pages/` — rasters
- `transcribed/` — question.txt + facts.yaml + deliverables.yaml
- `index.yaml` — parser lookup
